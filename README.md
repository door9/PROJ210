# PROJ210

매매의 **결과**가 아니라 **판단**을 기록하고, 시간이 지난 뒤 그 판단을 채점하는 개인 투자 복기 앱.
"내 계좌를 하나의 펀드로 보고, 나는 그 펀드의 매니저이자 유일한 고객"이라는 관점으로 만들었다.

## 기능

| 기능 | 설명 |
|---|---|
| 매매 기록 | 매수 시 이유·확신도·계획 보유기간·미리 정한 매도 조건·감정을 기록. 매도 시 "살 때의 나"가 다시 나타나 대조 |
| 평행우주 | 실제의 나 vs 손 안 댄 나 vs 코스피만 산 나 vs S&P500만 산 나 — 같은 매수를 한 네 세계의 현재 가치 비교 |
| 개입 점수 | 매도(판 뒤 그 주식이 어떻게 됐나)와 물타기(그 돈을 지수에 넣었다면)를 하나하나 채점 |
| 홀딩 일지 | "팔고 싶다/더 사고 싶다" 순간을 기록 → 몇 달 뒤 그 감정이 신호였는지 소음이었는지 채점 |
| 투자 헌법 | 원칙을 조문화(비중 한도·최소 보유일·물타기 금지는 자동 감시). 위반 시 경고하되 기록은 막지 않음. 원칙 자체도 검증(위반 매매 vs 준수 매매 수익률) |
| 주주 서한 | 분기마다 나에게 쓰는 운용보고서. 수익률·벤치마크·거래·위반·지난 서한을 자동 요약으로 차려줌 |
| AI 복기 | 기록 전체 + 심문 프롬프트를 데이터 팩으로 복사 → Claude에 붙여넣어 행동 패턴을 심문받기 |

## 구조 (개인정보 보호가 설계 기준)

- 바닐라 JS PWA (빌드 없음). **이 공개 저장소에는 앱 코드만 있다** — 매매 기록·종목명 등 개인 데이터는 없음.
- 사용자 데이터(매매·일지·헌법·서한): 브라우저 localStorage + **본인 Dropbox**(전용 앱 폴더의 `data.json`) 동기화.
  기기 간 병합은 항목별 updatedAt 엄격 비교(로컬이 확실히 새것일 때만 로컬 우선) + 삭제 tombstone.
- 시세·종목 목록: **비공개 저장소**(PROJ210-data)에 보관. 앱은 개인 접근 토큰(PAT)으로 GitHub API를 통해 읽고,
  새 종목 기록 시 tickers.json 커밋 + 갱신 워크플로 실행까지 자동.
  갱신: 정규장이 열려 있는 시장은 앱을 열 때마다(최소 2분 간격) 자동 갱신 트리거, 마감된 시장은 최근 종가 유지.
  안전망으로 매일 07:10·16:10 KST에도 자동 갱신(휴일 캘린더는 반영하지 않음 — 휴장일엔 그대로 재요청되지만 결과는 무해).
- 로컬 개발: `data/` 폴더가 있으면 폴백으로 사용(.gitignore 처리, `python scripts/fetch_prices.py 심볼`로 생성).

### 회계 가정 (engine.js)

- 모든 매수는 "새 돈" (매도 대금 재사용 미추적) / 매도 대금은 무이자 현금으로 잔류
- 평가액 = 매수원가 × 수정종가 성장배수 → 배당 재투자·액면분할 자동 반영
- 달러 자산은 해당일 원/달러 환율로 환산, 기준통화 KRW
- 네 평행우주 모두 같은 가정 → 비교는 공정

### 파일

```
index.html  style.css  manifest.json  sw.js  favicon.svg
js/  app.js(진입) core.js(라우터·모달) store.js(상태·예시데이터) prices.js(시세 조회: 비공개저장소→로컬 폴백)
     dropbox.js(OAuth·파일) sync.js(병합 동기화) engine.js(포트폴리오·평행우주·개입점수·헌법·데이터팩)
     chart.js(SVG차트) views-main.js(홈·기록) views-insight.js(평행우주·개입점수·일지) views-write.js(서한·헌법·AI·설정)
scripts/fetch_prices.py  (로컬 개발용 — 운영 수집은 PROJ210-data 저장소의 워크플로)
```

## 로컬 실행

```
python -m http.server 8735 --directory one-fund
```
